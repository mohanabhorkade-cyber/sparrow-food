# Image Preloading - Code Examples & Recipes

## Example 1: Basic Image Preloading\n\n```typescript\nimport { ImagePreloadService } from '@app/services/image-preload.service';\n\n@Component({\n  selector: 'app-gallery',\n  standalone: true\n})\nexport class GalleryComponent {\n  images = [\n    'assets/gallery/image1.webp',\n    'assets/gallery/image2.webp',\n    'assets/gallery/image3.webp'\n  ];\n\n  constructor(private imagePreloadService: ImagePreloadService) {}\n\n  ngOnInit() {\n    // Preload all gallery images on component load\n    this.imagePreloadService.preloadImages(this.images).catch(() => {\n      console.log('Some images failed to preload');\n    });\n  }\n}\n```\n\n## Example 2: Preload on User Interaction\n\n```typescript\n@Component({\n  selector: 'app-product-browser'\n})\nexport class ProductBrowserComponent {\n  products: Product[] = [];\n\n  constructor(private imagePreloadService: ImagePreloadService) {}\n\n  onMouseEnterProduct(product: Product) {\n    // Preload on hover for faster display\n    this.imagePreloadService.preloadImage(product.image).catch(() => {});\n  }\n\n  selectProduct(product: Product) {\n    // Immediate preload\n    this.imagePreloadService.preloadImage(product.image).catch(() => {});\n  }\n}\n```\n\n## Example 3: Smart Preload with Navigation\n\n```typescript\n@Component({\n  selector: 'app-carousel'\n})\nexport class CarouselComponent {\n  slides: Slide[] = [];\n  currentIndex = 0;\n\n  constructor(private imagePreloadService: ImagePreloadService) {}\n\n  ngOnInit() {\n    this.loadSlides();\n  }\n\n  private loadSlides() {\n    // Load slides from API\n    this.slides = [\n      { image: 'slide1.webp' },\n      { image: 'slide2.webp' },\n      { image: 'slide3.webp' }\n    ];\n\n    // Preload all slides\n    const images = this.slides.map(s => s.image);\n    this.imagePreloadService.preloadImages(images).catch(() => {});\n  }\n\n  nextSlide() {\n    this.currentIndex = (this.currentIndex + 1) % this.slides.length;\n    \n    // Smart preload next few slides\n    this.imagePreloadService.smartPreloadAround(\n      this.slides,\n      this.currentIndex,\n      3 // Preload 3 slides ahead and behind\n    ).catch(() => {});\n  }\n\n  previousSlide() {\n    this.currentIndex = (this.currentIndex - 1 + this.slides.length) % this.slides.length;\n    \n    this.imagePreloadService.smartPreloadAround(\n      this.slides,\n      this.currentIndex,\n      3\n    ).catch(() => {});\n  }\n}\n```\n\n## Example 4: Preload with Progress Tracking\n\n```typescript\n@Component({\n  selector: 'app-image-gallery-with-progress',\n  template: `\n    <div class=\"progress-bar\">\n      <div class=\"progress-fill\" [style.width.%]=\"preloadProgress\"></div>\n    </div>\n    <div class=\"gallery\">\n      <img *ngFor=\"let image of images\" [src]=\"image\" />\n    </div>\n  `\n})\nexport class GalleryWithProgressComponent implements OnInit {\n  images: string[] = [];\n  preloadProgress = 0;\n\n  constructor(private imagePreloadService: ImagePreloadService) {}\n\n  ngOnInit() {\n    // Subscribe to preload progress\n    this.imagePreloadService.getPreloadingProgress().subscribe(progress => {\n      this.preloadProgress = progress;\n      if (progress === 100) {\n        console.log('All images preloaded!');\n      }\n    });\n\n    // Start preloading\n    this.loadImages();\n  }\n\n  private loadImages() {\n    this.images = [\n      'assets/img1.webp',\n      'assets/img2.webp',\n      'assets/img3.webp'\n    ];\n\n    this.imagePreloadService.preloadImages(this.images).catch(() => {});\n  }\n}\n```\n\n## Example 5: Conditional Preloading (Mobile vs Desktop)\n\n```typescript\n@Component({\n  selector: 'app-responsive-gallery'\n})\nexport class ResponsiveGalleryComponent implements OnInit {\n  images: string[] = [];\n  isMobile = false;\n\n  constructor(private imagePreloadService: ImagePreloadService) {}\n\n  ngOnInit() {\n    this.checkIsMobile();\n    this.loadImages();\n  }\n\n  private checkIsMobile() {\n    this.isMobile = window.innerWidth < 768;\n  }\n\n  private loadImages() {\n    this.images = [\n      'assets/image1.webp',\n      'assets/image2.webp',\n      'assets/image3.webp',\n      'assets/image4.webp',\n      'assets/image5.webp'\n    ];\n\n    // On mobile: preload less aggressively\n    // On desktop: preload all images\n    if (this.isMobile) {\n      // Only preload current and next image\n      this.imagePreloadService.preloadImages(\n        this.images.slice(0, 2)\n      ).catch(() => {});\n    } else {\n      // Preload all images\n      this.imagePreloadService.preloadImages(this.images).catch(() => {});\n    }\n  }\n\n  onImageSelect(index: number) {\n    if (this.isMobile) {\n      // On mobile, dynamically preload next images as user navigates\n      const nextIndex = index + 1;\n      if (nextIndex < this.images.length) {\n        this.imagePreloadService.preloadImage(\n          this.images[nextIndex]\n        ).catch(() => {});\n      }\n    }\n  }\n}\n```\n\n## Example 6: Preload with Error Handling\n\n```typescript\n@Component({\n  selector: 'app-error-handling-gallery'\n})\nexport class ErrorHandlingGalleryComponent {\n  images: string[] = [];\n  failedImages: Set<string> = new Set();\n\n  constructor(private imagePreloadService: ImagePreloadService) {}\n\n  preloadImagesWithFallback() {\n    this.images = [\n      'assets/image1.webp',\n      'assets/image2.webp',\n      'assets/image3.webp'\n    ];\n\n    // Preload and track failures\n    this.images.forEach(image => {\n      this.imagePreloadService.preloadImage(image)\n        .then(() => {\n          console.log(`✓ Loaded: ${image}`);\n        })\n        .catch(() => {\n          console.warn(`✗ Failed to preload: ${image}`);\n          this.failedImages.add(image);\n        });\n    });\n  }\n\n  getImageUrl(image: string): string {\n    // Use fallback if image failed to load\n    return this.failedImages.has(image)\n      ? 'assets/fallback.webp'\n      : image;\n  }\n}\n```\n\n## Example 7: Cache Management\n\n```typescript\n@Component({\n  selector: 'app-cache-manager'\n})\nexport class CacheManagerComponent {\n  constructor(private imagePreloadService: ImagePreloadService) {}\n\n  // Check cache statistics\n  showCacheStats() {\n    const stats = this.imagePreloadService.getCacheStats();\n    console.log(`Cache size: ${stats.size} images`);\n    console.log('Cached URLs:', stats.urls);\n  }\n\n  // Clear cache when user logs out or navigates away\n  clearCache() {\n    this.imagePreloadService.clearCache();\n    console.log('Cache cleared');\n  }\n\n  // Selective preload based on cache status\n  preloadIfNotCached(imageUrl: string) {\n    if (!this.imagePreloadService.isCached(imageUrl)) {\n      this.imagePreloadService.preloadImage(imageUrl).catch(() => {});\n    } else {\n      console.log('Image already cached:', imageUrl);\n    }\n  }\n}\n```\n\n## Example 8: Using the Optional Preload Directive\n\n```typescript\nimport { PreloadImageDirective } from '@app/shared/preload-image.directive';\n\n@Component({\n  selector: 'app-directive-example',\n  standalone: true,\n  imports: [PreloadImageDirective],\n  template: `\n    <!-- Simple usage -->\n    <img [appPreloadImage]=\"productImage\" />\n\n    <!-- With smooth transition -->\n    <img \n      [appPreloadImage]=\"productImage\" \n      [smoothTransition]=\"true\"\n    />\n\n    <!-- With custom preload radius -->\n    <img \n      [appPreloadImage]=\"productImage\" \n      [preloadRadius]=\"5\"\n      [smoothTransition]=\"true\"\n    />\n  `\n})\nexport class DirectiveExampleComponent {\n  productImage = 'assets/product.webp';\n}\n```\n\n## Example 9: Preload in Routing Guards\n\n```typescript\nimport { CanActivate, Router } from '@angular/router';\nimport { ImagePreloadService } from '@app/services/image-preload.service';\n\n@Injectable({ providedIn: 'root' })\nexport class PreloadGuard implements CanActivate {\n  constructor(\n    private imagePreloadService: ImagePreloadService,\n    private router: Router\n  ) {}\n\n  canActivate(): Promise<boolean> {\n    // Preload images before allowing navigation\n    const images = [\n      'assets/page-banner.webp',\n      'assets/hero.webp'\n    ];\n\n    return this.imagePreloadService.preloadImages(images)\n      .then(() => true)\n      .catch(() => {\n        console.warn('Failed to preload images');\n        return true; // Allow navigation even if preload fails\n      });\n  }\n}\n```\n\n## Example 10: Analytics Integration\n\n```typescript\n@Component({\n  selector: 'app-performance-tracking'\n})\nexport class PerformanceTrackingComponent {\n  constructor(private imagePreloadService: ImagePreloadService) {}\n\n  async loadAndTrackImages(images: string[]) {\n    const startTime = performance.now();\n    const results: { image: string; time: number }[] = [];\n\n    for (const image of images) {\n      const imgStartTime = performance.now();\n      try {\n        await this.imagePreloadService.preloadImage(image);\n        const loadTime = performance.now() - imgStartTime;\n        results.push({ image, time: loadTime });\n      } catch (error) {\n        console.error(`Failed to load ${image}`);\n      }\n    }\n\n    const totalTime = performance.now() - startTime;\n    console.log('Performance Report:', {\n      totalTime: `${totalTime.toFixed(2)}ms`,\n      images: results,\n      averageTime: `${(totalTime / images.length).toFixed(2)}ms per image`\n    });\n\n    // Send to analytics service\n    this.sendAnalytics({\n      totalTime,\n      imageCount: images.length,\n      averageTime: totalTime / images.length\n    });\n  }\n\n  private sendAnalytics(data: any) {\n    // Send to your analytics service\n    console.log('Analytics data:', data);\n  }\n}\n```\n\n## Example 11: Lazy Scroll-based Preloading\n\n```typescript\nimport { OnScroll } from '@angular/cdk/scrolling';\n\n@Component({\n  selector: 'app-infinite-scroll-gallery',\n  standalone: true\n})\nexport class InfiniteScrollGalleryComponent implements OnScroll {\n  images: string[] = [];\n  private visibleIndex = 0;\n\n  constructor(private imagePreloadService: ImagePreloadService) {}\n\n  onScroll(event: CdkVirtualScrollIndexChange) {\n    this.visibleIndex = event.start;\n    \n    // Preload images around the visible area\n    const preloadRange = 5;\n    const startIndex = Math.max(0, this.visibleIndex - preloadRange);\n    const endIndex = Math.min(this.images.length, this.visibleIndex + preloadRange);\n\n    const imagesToPreload = this.images.slice(startIndex, endIndex);\n    this.imagePreloadService.preloadImages(imagesToPreload).catch(() => {});\n  }\n}\n```\n\n## Example 12: Preload with Network Status Check\n\n```typescript\n@Component({\n  selector: 'app-network-aware-preload'\n})\nexport class NetworkAwarePreloadComponent implements OnInit {\n  isOnline = navigator.onLine;\n\n  constructor(private imagePreloadService: ImagePreloadService) {}\n\n  ngOnInit() {\n    window.addEventListener('online', () => {\n      this.isOnline = true;\n      this.aggressivePreload(); // Preload when reconnected\n    });\n\n    window.addEventListener('offline', () => {\n      this.isOnline = false;\n      this.imagePreloadService.clearCache(); // Clear cache to save data\n    });\n  }\n\n  loadImages(images: string[]) {\n    if (this.isOnline) {\n      // Aggressive preloading when online\n      this.imagePreloadService.preloadImages(images).catch(() => {});\n    } else {\n      // No preloading when offline\n      console.log('Offline - skipping preload');\n    }\n  }\n\n  private aggressivePreload() {\n    const images = ['image1.webp', 'image2.webp', 'image3.webp'];\n    this.imagePreloadService.preloadImages(images).catch(() => {});\n  }\n}\n```\n\n---\n\n## 📌 Key Patterns\n\n### Always Use Error Handling\n```typescript\n// ✅ Good\nthis.imagePreloadService.preloadImages(urls).catch(() => {});\n\n// ❌ Bad - will crash if preload fails\nawait this.imagePreloadService.preloadImages(urls);\n```\n\n### Use Meaningful Progress Tracking\n```typescript\n// ✅ Good - shows user what's happening\nthis.imagePreloadService.getPreloadingProgress().subscribe(progress => {\n  this.loadingPercent = progress;\n});\n```\n\n### Clean Up on Component Destroy\n```typescript\nngOnDestroy() {\n  // Optional: Clear cache on destroy\n  this.imagePreloadService.clearCache();\n}\n```\n\n### Consider Mobile Networks\n```typescript\n// ✅ Good - adaptive preloading\nconst connection = (navigator as any).connection;\nif (connection?.effectiveType === '4g') {\n  // Aggressive preload on fast networks\n} else {\n  // Conservative preload on slow networks\n}\n```\n\n---\n\n**More examples and patterns available in ZERO_DELAY_IMAGE_LOADING_GUIDE.md**\n